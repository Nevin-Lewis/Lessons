//WRITE YOUR CODE BELOW
 var customer_order = {
    drink_name: "Espresso",
     sugars: 0, 
     ready: true
    };

console.log(customer_order.drink_name, customer_order.sugars)

if (customer_order.ready= true) {
console.log("Ready for pick up")}
else {
console.log("Still in order Queue")}


function order(drink_name, sugars, ready) {
    if (customer_order.ready= true)
    console.log(customer_order.drink_name + " Is ready for pick up")
    else
    console.log("Still in order Queue")
}

order("Cappuccino", 0,true)